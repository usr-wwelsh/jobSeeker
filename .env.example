# Copy to .env and fill in values — never commit .env

# Your GitHub username (used to fetch and index public repos)
GITHUB_USERNAME=your-github-username

# Password to access the dashboard
DASHBOARD_PASSWORD=your-secret-here

# Optional: raises GitHub API rate limit from 60 to 5000/hr
GITHUB_TOKEN=ghp_...
